PORT=8080
JWT_SECRET=replace-with-random-string-at-least-32-chars

# Use http://synapse:8008 when Synapse is in the same Docker network.
# Use http://host.docker.internal:8008 when Synapse runs on the host.
SYNAPSE_URL=http://synapse:8008

LAST_SEEN_FILE=/data/last_seen.json
AUTH_MODE=synapse
